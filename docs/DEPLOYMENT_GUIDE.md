# LangGraph Catalyst - デプロイガイド

## 目次
- [概要](#概要)
- [前提条件](#前提条件)
- [Renderデプロイ手順](#renderデプロイ手順)
- [環境変数設定](#環境変数設定)
- [デプロイ後の確認](#デプロイ後の確認)
- [トラブルシューティング](#トラブルシューティング)

---

## 概要

LangGraph Catalystは、Render上で以下の2つのサービスとしてデプロイされます：

| サービス | タイプ | URL |
|---------|--------|-----|
| **バックエンド** | Web Service (Python) | `https://langgraph-catalyst-api.onrender.com` |
| **フロントエンド** | Static Site (React) | `https://langgraph-catalyst-frontend.onrender.com` |

### アーキテクチャ
```
┌─────────────────────┐
│   ユーザー          │
└─────────┬───────────┘
          │ HTTPS
          ↓
┌─────────────────────┐
│ Frontend (React)    │ ← Render Static Site
│  - Vite Build       │
│  - Tailwind CSS     │
│  - Zustand (状態)   │
└─────────┬───────────┘
          │ API Calls (JWT認証)
          ↓
┌─────────────────────┐
│ Backend (FastAPI)   │ ← Render Web Service
│  - JWT認証          │
│  - 使用制限(5回/日) │
│  - LangChain        │
│  - LangGraph        │
│  - Chroma Vector DB │
└─────────┬───────────┘
          │
          ↓
┌─────────────────────┐
│ OpenAI API          │
└─────────────────────┘

データストア:
- Chroma: ベクトルDB（ローカルファイル）
- JSON: 使用制限カウンター（data/usage_limits.json）
- 環境変数: ユーザー情報（4ユーザー）
- localStorage: 学習進捗（フロントエンド）
```

---

## 前提条件

### 必須
- [x] GitHubアカウント
- [x] Renderアカウント（無料）
- [x] OpenAI APIキー
- [x] GitHubリポジトリに本プロジェクトをpush済み

### 推奨
- [x] カスタムドメイン（オプション、後で設定可能）
- [x] JWT秘密鍵の生成（認証使用時）

---

## Renderデプロイ手順

### ステップ1: GitHubリポジトリの準備

1. **リポジトリをGitHubにpush**
   ```bash
   git add .
   git commit -m "feat: Prepare for Render deployment"
   git push origin main
   ```

2. **ブランチの確認**
   - `main`ブランチにpushされていることを確認
   - render.yamlが最新版であることを確認

### ステップ2: Renderアカウント作成・ログイン

1. https://render.com にアクセス
2. "Get Started"をクリック
3. GitHubアカウントで認証
4. Renderダッシュボードに移動

### ステップ3: GitHubリポジトリ連携

1. Renderダッシュボードで "New" → "Blueprint" を選択
2. "Connect a repository"をクリック
3. GitHubリポジトリを検索・選択
4. `langgraph-catalyst`リポジトリを選択
5. "Connect"をクリック

### ステップ4: render.yaml の適用

1. Renderが自動的に`render.yaml`を検出
2. 以下の2つのサービスが表示されることを確認
   - `langgraph-catalyst-api` (Web Service)
   - `langgraph-catalyst-frontend` (Static Site)
3. "Apply"をクリック

### ステップ5: 環境変数の設定（重要）

render.yamlで`sync: false`と設定されている環境変数は、Render Dashboard上で**手動設定が必須**です。

#### バックエンド（langgraph-catalyst-api）

1. Renderダッシュボードで`langgraph-catalyst-api`サービスをクリック
2. 左メニューから "Environment" を選択
3. 以下の環境変数を追加（"Add Environment Variable"ボタン）

| 変数名 | 値 | 説明 |
|--------|-----|------|
| `OPENAI_API_KEY` | `sk-...` | OpenAI APIキー（必須） |
| `JWT_SECRET_KEY` | `<ランダム文字列>` | JWT署名用秘密鍵（認証必須） |
| `ADMIN_PASSWORD` | `<管理者パスワード>` | 管理者ユーザーのパスワード（認証必須） |
| `TESTUSER1_PASSWORD` | `<テストユーザー1パスワード>` | テストユーザー1のパスワード（認証必須） |
| `TESTUSER2_PASSWORD` | `<テストユーザー2パスワード>` | テストユーザー2のパスワード（認証必須） |
| `TESTUSER3_PASSWORD` | `<テストユーザー3パスワード>` | テストユーザー3のパスワード（認証必須） |

**JWT秘密鍵の生成方法**:
```bash
openssl rand -hex 32
```

**パスワードの要件**:
- 最低8文字以上を推奨
- 英数字 + 記号の組み合わせを推奨
- 環境変数に直接設定（bcryptハッシュ化は自動）

**ユーザー一覧**:
| ユーザー名 | ロール | 使用制限 |
|----------|-------|---------|
| `admin` | 管理者 | 無制限 |
| `testuser1` | 一般ユーザー | 5回/日 |
| `testuser2` | 一般ユーザー | 5回/日 |
| `testuser3` | 一般ユーザー | 5回/日 |

4. "Save Changes"をクリック

#### フロントエンド（langgraph-catalyst-frontend）

フロントエンドは環境変数がrender.yamlで自動設定されるため、手動設定は不要です。
- `VITE_API_BASE_URL`: 自動的にバックエンドURLが設定済み

### ステップ6: デプロイの実行

1. 環境変数設定後、自動的にデプロイが開始されます
2. "Events"タブでデプロイログを確認
3. ビルドステータスが"Live"になるまで待機（初回は5-10分程度）

#### バックエンドのビルドログ例
```
Installing dependencies...
pip install -r requirements.txt
Building...
Starting server...
uvicorn backend.main:app --host 0.0.0.0 --port $PORT
🚀 Starting LangGraph Catalyst API v1.0.0
   Environment: production
   CORS Origins: ['https://langgraph-catalyst-frontend.onrender.com', 'http://localhost:5173']
```

#### フロントエンドのビルドログ例
```
Installing dependencies...
cd frontend && npm ci
Building...
npm run build
✓ built in 18.52s
Deploying static site...
```

---

## 環境変数設定

### バックエンド環境変数一覧

| 変数名 | デフォルト値 | 説明 | 必須 |
|--------|------------|------|------|
| **認証関連** | | | |
| `JWT_SECRET_KEY` | - | JWT署名用秘密鍵（`openssl rand -hex 32`で生成） | ✅ |
| `JWT_ALGORITHM` | `HS256` | JWTアルゴリズム | - |
| `JWT_ACCESS_TOKEN_EXPIRE_MINUTES` | `1440` | トークン有効期限（24時間） | - |
| `ADMIN_PASSWORD` | - | 管理者パスワード（bcrypt自動ハッシュ化） | ✅ |
| `TESTUSER1_PASSWORD` | - | テストユーザー1パスワード | ✅ |
| `TESTUSER2_PASSWORD` | - | テストユーザー2パスワード | ✅ |
| `TESTUSER3_PASSWORD` | - | テストユーザー3パスワード | ✅ |
| **OpenAI API** | | | |
| `OPENAI_API_KEY` | - | OpenAI APIキー | ✅ |
| `DEFAULT_LLM_MODEL` | `gpt-4-turbo-preview` | LLMモデル | - |
| `DEFAULT_EMBEDDING_MODEL` | `text-embedding-3-small` | 埋め込みモデル | - |
| `MAX_TOKENS` | `4096` | 最大トークン数 | - |
| `TEMPERATURE` | `0.3` | LLM温度パラメータ | - |
| **インフラ設定** | | | |
| `CHROMA_PERSIST_DIR` | `./data/chroma` | Chromaベクトルストアのパス | - |
| `CORS_ORIGINS` | `https://langgraph-catalyst-frontend.onrender.com,http://localhost:5173` | CORS許可オリジン | - |
| `LOG_LEVEL` | `INFO` | ログレベル（DEBUG/INFO/WARNING/ERROR） | - |
| `ENVIRONMENT` | `production` | 実行環境（production/development） | - |

### フロントエンド環境変数一覧

| 変数名 | デフォルト値 | 説明 | 必須 |
|--------|------------|------|------|
| `VITE_API_BASE_URL` | `https://langgraph-catalyst-api.onrender.com/api/v1` | バックエンドAPIのURL | ✅ |

---

## デプロイ後の確認

### 1. ヘルスチェック

バックエンドが正常に起動しているか確認：

```bash
curl https://langgraph-catalyst-api.onrender.com/health
```

**期待される応答**:
```json
{
  "status": "healthy",
  "version": "1.0.0",
  "environment": "production"
}
```

### 2. API ドキュメント確認

Swagger UIにアクセスして、APIドキュメントを確認：

- **Swagger UI**: https://langgraph-catalyst-api.onrender.com/docs
- **ReDoc**: https://langgraph-catalyst-api.onrender.com/redoc

### 3. フロントエンド確認

1. https://langgraph-catalyst-frontend.onrender.com にアクセス
2. ホームページが正常に表示されることを確認
3. ログインページ（/login）にアクセス可能か確認
4. ナビゲーション（Home, RAG, LearningPath, Templates, Architect）が動作するか確認

### 4. CORS設定確認

ブラウザのDevToolsを開き、Consoleタブで以下を確認：

- ✅ CORS errorが**表示されない**こと
- ✅ APIリクエストが正常に送信されること

CORS errorが出た場合:
1. バックエンドの`CORS_ORIGINS`環境変数を確認
2. フロントエンドのURLが正しく設定されているか確認
3. `https://langgraph-catalyst-frontend.onrender.com`が含まれているか確認

### 5. 認証フロー確認

1. ログインページで管理者アカウントでログイン
   - Username: `admin`
   - Password: `ADMIN_PASSWORD`環境変数に設定した値
2. ログイン成功後、ダッシュボードにリダイレクトされることを確認
3. RAGページ、Architectページにアクセス可能か確認
4. ログアウトが正常に動作するか確認

### 6. 機能テスト

#### 認証機能
1. ログインページ（`/login`）にアクセス
2. **管理者アカウントでログイン**
   - Username: `admin`
   - Password: `ADMIN_PASSWORD`環境変数に設定した値
3. ログイン成功後、ホームページにリダイレクトされることを確認
4. ヘッダーにユーザー名が表示されることを確認

#### RAG機能
1. RAGページにアクセス（認証必要）
2. 「LangGraphとは何ですか？」と質問
3. ソース付きで回答が返ってくることを確認
4. 使用回数が正しくカウントされているか確認（テストユーザーの場合）

#### 構成案生成機能
1. Architectページにアクセス（認証必要）
2. ビジネス課題を入力（例: 「カスタマーサポートの自動化」）
3. Mermaid図、コード例、説明が生成されることを確認

#### 学習パス機能
1. Learning Pathページにアクセス（認証不要）
2. 初級/中級/上級のトピックが表示されることを確認
3. トピック完了チェックボックスが動作するか確認
4. 進捗バーが正しく更新されるか確認

#### テンプレート機能
1. Templatesページにアクセス（認証不要）
2. カテゴリ別にテンプレートが表示されることを確認
3. 難易度フィルタが動作するか確認
4. コード例が正しく表示されるか確認

#### 使用制限機能
1. **テストユーザー1でログイン**
   - Username: `testuser1`
   - Password: `TESTUSER1_PASSWORD`環境変数に設定した値
2. RAGまたはArchitect機能を5回使用
3. 6回目のリクエストで「本日の使用回数上限に達しました」エラーが表示されることを確認
4. ヘッダーに「残り0回」が表示されることを確認

---

## トラブルシューティング

### 問題1: バックエンドが起動しない

**症状**: `/health`エンドポイントにアクセスできない

**原因と対策**:
1. **環境変数不足**
   - `OPENAI_API_KEY`が設定されているか確認
   - Render Dashboard → Environment で確認

2. **ビルドエラー**
   - Render Dashboard → Events でビルドログを確認
   - `requirements.txt`の依存関係エラーがないか確認

3. **ポート設定ミス**
   - `startCommand`が`--port $PORT`を含んでいるか確認
   - render.yamlの設定を確認

### 問題2: フロントエンドからAPIにアクセスできない

**症状**: ブラウザコンソールに「CORS error」が表示される

**原因と対策**:
1. **CORS_ORIGINS設定ミス**
   ```bash
   # 正しい設定例
   CORS_ORIGINS=https://langgraph-catalyst-frontend.onrender.com,http://localhost:5173
   ```

2. **HTTPSとHTTPの混在**
   - フロントエンドがHTTPSでデプロイされている場合、バックエンドもHTTPSである必要がある
   - Renderは自動的にHTTPSを提供するので、通常は問題なし

3. **APIベースURLのミス**
   - フロントエンドの`VITE_API_BASE_URL`が正しいか確認
   - `/api/v1`サフィックスが含まれているか確認

### 問題3: ログインできない

**症状**: 「認証失敗」エラーが表示される

**原因と対策**:
1. **パスワード設定ミス**
   - `ADMIN_PASSWORD`等の環境変数が正しく設定されているか確認
   - Render Dashboard → Environment で確認

2. **JWT秘密鍵未設定**
   - `JWT_SECRET_KEY`が設定されているか確認
   - ランダム文字列が設定されているか確認

3. **ユーザー名のスペルミス**
   - ユーザー名: `admin`, `testuser1`, `testuser2`, `testuser3` が正しいか確認

### 問題4: デプロイが遅い/失敗する

**症状**: デプロイが10分以上かかる、または失敗する

**原因と対策**:
1. **依存関係のインストールエラー**
   - ローカルで`pip install -r requirements.txt`を実行してエラーがないか確認
   - Python バージョンが3.10以上か確認

2. **npm依存関係エラー**
   - ローカルで`npm ci`を実行してエラーがないか確認
   - `package-lock.json`が最新か確認

3. **ビルドタイムアウト**
   - Render無料プランではビルド時間に制限があります
   - 大きな依存関係を削減するか、有料プランを検討

### 問題5: Chromaベクトルストアが永続化されない

**症状**: デプロイ後、RAGデータが消えている

**原因と対策**:
1. **永続ディスク未設定**
   - Render無料プランでは再起動時にデータが消える
   - render.yamlの`disk`設定をコメントアウト解除（有料プラン必要）
   ```yaml
   disk:
     name: chroma-data
     mountPath: /data/chroma
     sizeGB: 1
   ```

2. **CHROMA_PERSIST_DIR設定ミス**
   - `./data/chroma`が正しいパスか確認
   - マウントパスと一致しているか確認

### 問題6: コールドスタート（初回アクセスが遅い）

**症状**: しばらくアクセスしないと、初回アクセスが30秒以上かかる

**原因**: Render無料プランでは、15分間アクセスがないとサービスがスリープします

**対策**:
1. **有料プランにアップグレード**（$7/月〜）
   - スリープなしで常時起動
   - より高速なCPU/メモリ

2. **ウォームアップスクリプト**（無料プランで継続使用）
   - 定期的にヘルスチェックを叩くスクリプトを外部サービス（UptimeRobot等）で実行
   - 15分ごとにアクセスしてスリープを防ぐ

### 問題7: 使用制限カウンターがリセットされない

**症状**: 日付が変わっても使用回数がリセットされない、またはリセットされすぎる

**原因**:
1. サーバーのタイムゾーンが異なる（Render無料プランは通常UTC）
2. Render再起動により`data/usage_limits.json`が消失

**対策**:
1. **タイムゾーン確認**
   ```bash
   # ローカルで確認
   python -c "from datetime import date; print(date.today())"
   ```

2. **使用制限カウンターの手動リセット**（管理者のみ）
   - Renderシェルで`data/usage_limits.json`を削除
   ```bash
   rm -f data/usage_limits.json
   ```

3. **Render無料プランの制約を理解**
   - 再起動時にファイルが消える可能性がある
   - 永続化が必要な場合は有料プランのDisk機能を使用

### 問題8: 「本日の使用回数上限に達しました」エラーが出るが、実際は使用していない

**症状**: テストユーザーで初めてログインしたのに、すぐに429エラーが表示される

**原因**:
1. 別のユーザーが同じアカウントを使用している
2. `data/usage_limits.json`のカウントが正しく更新されていない
3. サーバーが再起動せず、前日のカウントが残っている

**対策**:
1. **使用制限カウンターの確認**（管理者のみ）
   - Renderシェルで`data/usage_limits.json`の内容を確認
   ```bash
   cat data/usage_limits.json
   ```

2. **日付が正しいか確認**
   - カウンター内のdateフィールドが今日の日付（YYYY-MM-DD）か確認

3. **カウンターの手動リセット**
   ```bash
   rm -f data/usage_limits.json
   ```

---

## カスタムドメイン設定（オプション）

### 1. ドメインの取得

- Google Domains, Cloudflare, Namecheap等でドメインを取得

### 2. フロントエンドのカスタムドメイン設定

1. Render Dashboard → `langgraph-catalyst-frontend` → Settings → Custom Domain
2. "Add Custom Domain"をクリック
3. 例: `app.yourdomain.com`を入力
4. Renderが提示するCNAMEレコードをDNS設定に追加
   ```
   CNAME: app.yourdomain.com → langgraph-catalyst-frontend.onrender.com
   ```
5. SSL証明書が自動発行されるまで待機（5-10分）

### 3. バックエンドのカスタムドメイン設定

1. Render Dashboard → `langgraph-catalyst-api` → Settings → Custom Domain
2. "Add Custom Domain"をクリック
3. 例: `api.yourdomain.com`を入力
4. DNSにCNAMEレコードを追加
   ```
   CNAME: api.yourdomain.com → langgraph-catalyst-api.onrender.com
   ```

### 4. CORS設定の更新

カスタムドメイン設定後、CORS設定を更新：

1. バックエンドの環境変数`CORS_ORIGINS`を更新
   ```
   CORS_ORIGINS=https://app.yourdomain.com,http://localhost:5173
   ```

2. フロントエンドの環境変数`VITE_API_BASE_URL`を更新
   ```
   VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
   ```

3. Render Dashboard → Deploy Manual Deploy で再デプロイ

---

## パフォーマンス最適化

### 1. フロントエンドビルド最適化

現在のビルド警告:
```
(!) Some chunks are larger than 500 kB after minification.
```

**対策**:
1. **コード分割（Code Splitting）**
   - 動的インポートを使用
   ```typescript
   const MermaidDiagram = lazy(() => import('./components/Mermaid/MermaidDiagram'));
   ```

2. **バンドルサイズ分析**
   ```bash
   npm run build -- --mode analyze
   ```

3. **不要な依存関係の削除**
   - 使用していないライブラリを削除

### 2. キャッシング設定

render.yamlで既に設定済み:
```yaml
headers:
  - path: /*
    name: Cache-Control
    value: public, max-age=0, must-revalidate  # HTML
  - path: /assets/*
    name: Cache-Control
    value: public, max-age=31536000, immutable  # 静的アセット
```

### 3. Lighthouseスコア目標

| 項目 | 目標 | 現在 |
|------|------|------|
| Performance | 85+ | TBD |
| Accessibility | 95+ | TBD |
| Best Practices | 95+ | TBD |
| SEO | 90+ | TBD |

**Lighthouse実行方法**:
1. Chrome DevTools → Lighthouse タブ
2. "Generate report"をクリック
3. スコアを確認し、改善提案に従う

---

## 次のステップ

1. [x] Renderデプロイ完了
2. [ ] カスタムドメイン設定
3. [ ] パフォーマンス最適化
4. [ ] Lighthouseスコア改善
5. [ ] ポートフォリオサイトにリンク追加
6. [ ] 技術ブログ記事作成

---

## 参考リンク

- [Render公式ドキュメント](https://render.com/docs)
- [Render Blueprint仕様](https://render.com/docs/blueprint-spec)
- [FastAPIデプロイガイド](https://fastapi.tiangolo.com/deployment/)
- [Vite本番ビルド](https://vitejs.dev/guide/build.html)

---

**Last Updated**: 2026-02-07
